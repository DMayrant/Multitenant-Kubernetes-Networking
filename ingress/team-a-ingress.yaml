apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: team-a-ingress
  namespace: team-a
spec:
  rules:
  - host: team-a.policy.com
    http:
      paths:
      - backend:
          service:
            name: team-a-app
            port:
              number: 80
        path: /
        pathType: Exact
status:
  loadBalancer: {}
